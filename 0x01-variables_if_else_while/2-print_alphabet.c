#include <stdio.h>

/**
* main - prints the alphabet in lowercase
*
* Return: always 0
*/
int main(void)
{
	char alphabet[26] = "abcdefghijklmnopqrstuvwxyz"
		int a;
	
	for (a = 0 ; a < 26 ; a++)
		putchar(char alphabet)(a);
	putchar('\n');

	return (0);
}
