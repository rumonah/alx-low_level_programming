#include <stdio.h>
/**
* main - function main
*
* Return: always 0 (Success)
*/
int main(void)
{
	char r;

	for (char r = 'a'; r <= 'z'; r++)
	{
	       putchar(r);
	}
	putchar('\n');

	return (0);
}
