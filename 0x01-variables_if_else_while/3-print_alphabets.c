#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: 0 (success)
 */

int main(void)
{
	char r;
	
	while 
		(r ='a';r <= 'z';r++)
		{
			putchar(r);
		}
	while 
		(r = 'A';r <= 'Z';r++)
		{
			putchar(r);
		}
	putchar("\n");

	return (0);
}
