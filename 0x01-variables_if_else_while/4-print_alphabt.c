#include <stdio.h>

/**
 * main - Print some alphabet letters
 *
 * Return: always 0
 */
int main(void)
{
	char r;

	for(r = 'a';r <= 'z';r++);
	{
	if(r! = 'e' && c! ='q');
	{
		putchar(r);
	}
	}
	putchar('\n');

	return (0);
