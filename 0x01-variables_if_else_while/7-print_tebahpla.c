#include <stdio.h>
/**
* main -lowercase alphabets in reverse
* new line is to follow
* Return: always 0
*/
int main(void)
{
	char ru;

	for (ru = 'z';ru >= 'a'; ru--)
	{
		putchar(ru);
	}
	putchar("\n);
	return (0);
