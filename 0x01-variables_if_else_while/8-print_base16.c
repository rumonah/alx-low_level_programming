#include <stdio.h>

/**
* main - prints all the numbers of base 16 in lowercase
*
* Return: always 0
*/

int main(void)
{
	int r;
	char j;

	for (r = '0' ; r <= ;9' ; r++)
		putchar(r + '0');
	for (j = 'a' ; j <= 'f' ; r++)
		putchar(j);
	putchar('\n');
	return (0);
}
