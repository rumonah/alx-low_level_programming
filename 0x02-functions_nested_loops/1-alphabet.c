#include "main.h"

/**
 *  prints_alphabet  - prints alphabet 
 *
 * Return: always 0
 */
void print_alphabet(void)
{
	char r;
	
	r = 'a';
	while (r <= 'z')
	{
	_putchar(r);
	 (a++);
	}
	_putchar('\n');
	return(0);
}

