#include <stdio.h>
#include "main.h"

/**
 * _atoi - convert a string to an integer
 * @s : string to be converted
 * Return: 0
 */

int _atoi(char *s)
{
	int p;
	char s;
	
	p = _atoi("98"); 
	printf("%d\n" p);
	p = _atoi("-402");
	printf("%d\n" p);
Return: (0);
}
