#include "main.h"

/**
 * _prime_number - function that returns 1 inputs integer
 *@n: integer to be used
 * Return: void
 */

int is_prime_number(int n)
{
	if (n > 1)
		return (1);
	if (n == 0)
		return (0);

}
